# 🚀 COMMERCE MVP

> **Мы монетизируем ваши связи** — B2B платформа для промышленной коммерции

Telegram Mini App для автоматизации B2B посредничества: подбор контрагентов, сопровождение сделок, комиссия 3% с успешных сделок.

---

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Архитектура](#-архитектура)
- [Развертывание](#-развертывание)
- [API документация](#-api-документация)
- [Telegram Bot](#-telegram-bot)
- [Финансовая модель](#-финансовая-модель)

---

## ⚡ Быстрый старт

### Требования
- Node.js 18+
- PostgreSQL 14+
- Redis 7+
- Telegram Bot Token

### Локальная установка

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/your-org/commerce-mvp.git
cd commerce-mvp

# 2. Установите зависимости
npm install

# 3. Настройте окружение
cp .env.example .env
# Отредактируйте .env файл

# 4. Создайте базу данных
createdb commerce
psql commerce < database/schema.sql

# 5. Запустите сервер
npm run dev
```

### Настройка Telegram Bot

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен и добавьте в `.env`
3. Настройте Web App URL:
   ```
   /setmenubutton → Web App URL → https://your-app.vercel.app
   ```

---

## 🏗 Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                      TELEGRAM                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │  Mini App    │  │     Bot      │  │  Notifications│       │
│  │   (React)    │  │  (Telegraf)  │  │   (Push)     │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
└─────────┼─────────────────┼─────────────────┼───────────────┘
          │                 │                 │
          ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────┐
│                     API GATEWAY                              │
│                   (Express + Auth)                           │
└─────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │   Requests   │  │   Matching   │  │    Deals     │       │
│  │   Service    │  │    Engine    │  │   Service    │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────┐
│                      DATA LAYER                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │  PostgreSQL  │  │    Redis     │  │      S3      │       │
│  │   (Data)     │  │   (Cache)    │  │   (Files)    │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

### Структура проекта

```
commerce-mvp/
├── src/
│   ├── index.js           # Entry point
│   ├── bot.js             # Telegram bot
│   ├── routes/            # API routes
│   ├── services/          # Business logic
│   ├── models/            # Data models
│   └── utils/             # Helpers
├── frontend/
│   ├── src/
│   │   ├── App.jsx        # Mini App
│   │   ├── components/    # React components
│   │   └── hooks/         # Custom hooks
│   └── public/
├── database/
│   ├── schema.sql         # DB schema
│   └── migrations/        # Migrations
├── landing/
│   └── index.html         # Landing page
└── docs/
    └── api.md             # API docs
```

---

## 🚀 Развертывание

### Option 1: Vercel + Railway (Рекомендуется для MVP)

**Frontend (Vercel):**
```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
cd frontend
vercel --prod
```

**Backend (Railway):**
1. Создайте проект на [railway.app](https://railway.app)
2. Добавьте PostgreSQL и Redis
3. Подключите GitHub репозиторий
4. Настройте переменные окружения

### Option 2: Docker

```dockerfile
# Dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 3000
CMD ["node", "src/index.js"]
```

```yaml
# docker-compose.yml
version: '3.8'
services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/commerce
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis
      
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: commerce
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
      
  redis:
    image: redis:7-alpine
    
volumes:
  pgdata:
```

### Option 3: VPS (Ubuntu)

```bash
# 1. Установите зависимости
sudo apt update
sudo apt install -y nodejs npm postgresql redis-server nginx

# 2. Настройте PostgreSQL
sudo -u postgres createuser commerce
sudo -u postgres createdb commerce -O commerce

# 3. Клонируйте и настройте
git clone https://github.com/your-org/commerce-mvp.git
cd commerce-mvp
npm install
cp .env.example .env
nano .env

# 4. Запустите через PM2
npm install -g pm2
pm2 start src/index.js --name commerce-api
pm2 save
pm2 startup

# 5. Настройте Nginx
sudo nano /etc/nginx/sites-available/commerce
```

```nginx
server {
    listen 80;
    server_name api.commerce.io;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## 📚 API Документация

### Аутентификация

Все запросы требуют заголовок:
```
X-Telegram-User-Id: <telegram_user_id>
```

### Endpoints

#### Заявки (Requests)

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | `/api/requests` | Список заявок |
| GET | `/api/requests/my` | Мои заявки |
| POST | `/api/requests` | Создать заявку |
| GET | `/api/requests/:id` | Детали заявки |
| PUT | `/api/requests/:id` | Обновить заявку |
| GET | `/api/requests/:id/matches` | Матчи для заявки |

#### Сделки (Deals)

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | `/api/deals/my` | Мои сделки |
| POST | `/api/deals` | Создать сделку |
| PUT | `/api/deals/:id/stage` | Обновить этап |

#### Пример запроса

```bash
# Создание заявки
curl -X POST https://api.commerce.io/api/requests \
  -H "Content-Type: application/json" \
  -H "X-Telegram-User-Id: 123456789" \
  -d '{
    "type": "buy",
    "category_id": 1,
    "title": "Арматура А500С d12",
    "volume": "50 тонн",
    "budget_min": 2000000,
    "budget_max": 2500000,
    "region_id": 1
  }'
```

---

## 🤖 Telegram Bot

### Команды

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы |
| `/new` | Новая заявка |
| `/my` | Мои заявки |
| `/deals` | Мои сделки |
| `/help` | Помощь |

### Webhooks

Для production настройте webhook:
```bash
curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://api.commerce.io/bot/webhook"}'
```

---

## 💰 Финансовая модель

### Источники дохода

| Источник | Ставка | Описание |
|----------|--------|----------|
| **Комиссия** | 3% | С суммы успешной сделки |
| Premium | ₽9,900/мес | Приоритет в матчинге |
| Boost | ₽2,000-10,000 | Продвижение заявки |
| Верификация | ₽5,000 | Проверка компании |

### Прогноз (12 месяцев)

| Период | Сделок | Оборот | Комиссия |
|--------|--------|--------|----------|
| Q1 | 15 | ₽45M | ₽1.35M |
| Q2 | 40 | ₽160M | ₽4.8M |
| Q3 | 80 | ₽400M | ₽12M |
| Q4 | 120 | ₽720M | ₽21.6M |
| **Итого** | **255** | **₽1.325B** | **₽39.75M** |

### Unit-экономика

```
Средний чек:         ₽5,000,000
Комиссия (3%):       ₽150,000
- Эквайринг (1.5%):  -₽2,250
- Операционные:      -₽5,000
─────────────────────────────
Чистая маржа:        ₽142,750 (95%)
```

---

## 🛠 Разработка

### Запуск тестов
```bash
npm test
```

### Линтинг
```bash
npm run lint
```

### Миграции
```bash
npm run db:migrate
```

---

## 📞 Контакты

- **Telegram:** [@commerce_support](https://t.me/commerce_support)
- **Email:** hello@commerce.io
- **Website:** [commerce.io](https://commerce.io)

---

## 📄 Лицензия

MIT © 2025 Commerce Team
